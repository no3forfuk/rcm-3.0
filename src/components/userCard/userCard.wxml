<view class="user-card-component" style="align-items: {{alignItems}}">
    <image src="{{userInfo.avatar}}"
           mode="aspectFill"
           style="width:{{avatarWidth}}rpx;height: {{avatarHeight}}rpx;border-radius: 50%;"></image>
    <slot></slot>
</view>